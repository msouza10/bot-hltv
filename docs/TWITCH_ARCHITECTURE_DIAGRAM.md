# 📊 System Architecture: Twitch Auto-Search Integration

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    DISCORD BOT STARTUP                      │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │ CacheScheduler.start()             │
        │ - update_all_task                  │
        │ - check_finished_task              │
        │ - populate_streams_task ← NEW!     │
        └────────────────┬───────────────────┘
                         │
        ┌────────────────▼──────────────────┐
        │      LOOP EVERY 10 MINUTES        │
        │  populate_streams_task()          │
        │      (NEW FEATURE)                │
        └────────────────┬──────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │ Query Database:                    │
        │ SELECT matches with:               │
        │ - status IN (running, not_started) │
        │ - NO raw_url (no official stream)  │
        │ LIMIT 20                           │
        └────────────────┬──────────────────┘
                         │
              ┌──────────┴──────────┐
              │ For each match:     │
              │ - Get championship  │
              │ - Get team1, team2  │
              └──────────┬──────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │ twitch_service.search_streams()    │
        │ - Query Twitch API                 │
        │ - Find available streams           │
        │ - Score & rank by relevance        │
        └────────────────┬──────────────────┘
                         │
              ┌──────────▼──────────┐
              │ Stream found?       │
              └──┬──────────────┬───┘
              YES               NO
                │                │
                ▼                ▼
            INSERT          Log: no
            (3)              match


┌─────────────────────────────────────────────────────────────┐
│                    (3) INSERT INTO DB                        │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │ cache_streams()                    │
        │ INSERT INTO match_streams:         │
        │                                    │
        │ + match_id                         │
        │ + platform: "twitch"               │
        │ + channel_name: "gaules"           │
        │ + url: link                        │
        │ + raw_url: link                    │
        │ + language: "pt"                   │
        │ + is_official: false               │
        │ + is_main: false                   │
        │ + is_automated: TRUE ← NEW!        │
        │ + viewer_count: 2847 ← NEW!        │
        │ + title: "ESL..." ← NEW!           │
        └────────────────┬──────────────────┘
                         │
                         ▼
    ✅ Stream cached with automation flag


┌─────────────────────────────────────────────────────────────┐
│              WHEN USER REQUESTS MATCH                        │
│            (e.g., /partidas or /aovivo)                     │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │ get_cached_matches()               │
        │ Retrieves match data with status   │
        └────────────────┬──────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │ get_match_streams(match_id)        │
        │ SELECT FROM match_streams:         │
        │ + platform, channel_name           │
        │ + url, raw_url, language           │
        │ + is_official, is_main             │
        │ + is_automated ← READ NEW!         │
        │ + viewer_count ← READ NEW!         │
        │ + title ← READ NEW!                │
        └────────────────┬──────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │ format_streams_field()             │
        │ Normalize streams:                 │
        │ - Check is_automated flag          │
        │ - Add 🤖 emoji if automated        │
        │ - Add 🇧🇷 language flag             │
        │ - Add ⭐ if official               │
        │ - Create hyperlink                 │
        └────────────────┬──────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │ create_match_embed()               │
        │ - Add streams field                │
        │ - Display with all flags           │
        │ - Add thumbnail & colors           │
        └────────────────┬──────────────────┘
                         │
                         ▼
    ✅ Embed sent to Discord with 🤖 emoji
```

## Database Schema Changes

```
┌──────────────────────────────────┐
│     match_streams TABLE          │
├──────────────────────────────────┤
│ BEFORE:                          │
│ - id                             │
│ - match_id                       │
│ - platform                       │
│ - channel_name                   │
│ - url                            │
│ - raw_url                        │
│ - language                       │
│ - is_official                    │
│ - is_main                        │
│ - cached_at                      │
│ - updated_at                     │
│                                  │
│ AFTER: (3 NEW COLUMNS)           │
│ + is_automated ← NEW (AUTO=0)    │
│ + viewer_count ← NEW (AUTO=0)    │
│ + title ← NEW (AUTO=NULL)        │
└──────────────────────────────────┘
```

## Time-Based Execution

```
BOT STARTUP
    │
    ├─ T+0: Initialize Discord connection
    ├─ T+1: Load database schema
    ├─ T+2: Start CacheScheduler
    │   ├─ update_all_task runs every 3 minutes → every 3 min
    │   ├─ check_finished_task runs every 1 minute → every 1 min
    │   └─ populate_streams_task runs every 10 min → every 10 min
    │
    └─ T+5: populate_streams_task.before_loop waits...
            (Gives cache time to stabilize)
            
FIRST AUTO-SEARCH: ~T+10 minutes
    │
    ├─ Query: Find matches without streams
    ├─ For each: Search Twitch
    ├─ If found: Cache with is_automated=True
    └─ Log: 🤖 ✓ Streams found

SUBSEQUENT CYCLES: Every 10 minutes
    │
    ├─ T+20: populate_streams_task runs again
    ├─ T+30: ...
    └─ T+N: ... (continues throughout bot lifetime)

USER REQUEST (any time)
    └─ /partidas → Shows matches with 🤖 emoji
```

## Component Interaction

```
┌────────────────────────────────────────────────┐
│                   cache_scheduler.py           │
│  ┌──────────────────────────────────────────┐ │
│  │ populate_streams_task()                  │ │
│  │ - Runs every 10 min (Discord Task)       │ │
│  │ - Calls populate_missing_streams()       │ │
│  └───────────────┬──────────────────────────┘ │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────┐
│            twitch_search_service.py            │
│  ┌──────────────────────────────────────────┐ │
│  │ TwitchSearchService.search_streams()     │ │
│  │ - Queries Twitch API                     │ │
│  │ - Returns stream data with:              │ │
│  │   * url, channel_name, viewer_count      │ │
│  │   * language, title, is_automated=true   │ │
│  └───────────────┬──────────────────────────┘ │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────┐
│             cache_manager.py                   │
│  ┌──────────────────────────────────────────┐ │
│  │ cache_streams(match_id, streams_list)    │ │
│  │ - Processes all fields                   │ │
│  │ - Handles is_automated, viewer_count     │ │
│  │ - INSERTs into database                  │ │
│  └──────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────┐ │
│  │ get_match_streams(match_id)              │ │
│  │ - Retrieves all fields including:        │ │
│  │   * is_automated, viewer_count, title    │ │
│  │ - Returns normalized dict                │ │
│  └───────────────┬──────────────────────────┘ │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────┐
│               embeds.py                        │
│  ┌──────────────────────────────────────────┐ │
│  │ format_streams_field(streams)            │ │
│  │ - Normalizes stream data                 │ │
│  │ - Preserves is_automated flag            │ │
│  │ - Adds 🤖 emoji if is_automated=true     │ │
│  │ - Returns formatted string               │ │
│  └──────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────┐ │
│  │ create_match_embed()                     │ │
│  │ - Calls format_streams_field()           │ │
│  │ - Adds streams to embed field            │ │
│  │ - Sends to Discord                       │ │
│  └───────────────┬──────────────────────────┘ │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
            ✅ Discord User
         sees 🤖 emoji in embed
```

## Configuration Flow

```
.env
 ├─ TWITCH_CLIENT_ID
 ├─ TWITCH_CLIENT_SECRET
 ├─ LIBSQL_URL
 └─ LIBSQL_AUTH_TOKEN
        │
        ▼
   TwitchSearchService()
   ├─ Uses CLIENT_ID + SECRET
   ├─ Gets OAuth token
   └─ Queries Twitch API
        │
        ▼
   MatchCacheManager()
   ├─ Uses LIBSQL_URL + TOKEN
   ├─ Connects to database
   └─ Stores/retrieves streams
        │
        ▼
   ✅ Full integration ready
```

## Status Indicators in Logs

```
✅ Success indicators:
   - 🤖 Iniciando busca automática... (task started)
   - 🔍 Encontrados X matches (found matches without streams)
   - ✅ Stream encontrada: [name] (X viewers) (success!)

⚠️ Warning indicators:
   - ⚠️ Nenhum stream para match (no results)
   - ❌ Nenhuma stream encontrada (not found)
   - ⏱️ Timeout ao buscar streams (timeout)

🔧 Config indicators:
   - • Busca automática de streams: a cada 10 minutos (active)
   - TWITCH_CLIENT_ID not set (missing config)
   - TWITCH_CLIENT_SECRET not set (missing config)
```

---

This complete architecture ensures:
- ✅ Automatic stream search every 10 minutes
- ✅ Persistent caching with all metadata
- ✅ Clear user indication of automated streams (🤖)
- ✅ Zero impact on existing functionality
- ✅ Graceful degradation on failures
